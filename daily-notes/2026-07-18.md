---
aliases:
title: 2026-07-18
tags:
  - daily-note
  - ai-generated
---
# 2026-07-18
[[2026-07-17]]<- [[2026-07-18]] -> [[2026-07-19]]

## Session Notes

### Close the Positive Lower Bound, Centered Product, and Positive-Definite Orthant

**Goal:** Complete WP16–WP18 and advance immediately into the singular outer approximation.

**Changes:**

- Added `Gaussian/DensityRatio.lean` with the exact positive-definite density identity, compact-box
  positive minimum, and deterministic real box-integral inequality.
- Added `Product/PositiveLowerBound.lean` with scalar Portmanteau plumbing kept private and the
  eventual positive lower bound for every dyadic correlated product integral.
- Added `Product/CenteredProduct.lean`, proving `centered_product_of_posDef` by normalization,
  repeated squaring, contradiction against the fixed lower bound, and denormalization. This closes
  M2.
- Added `Orthant/PositiveDefinite.lean`, combining the conditional M1 theorem with M2 to prove
  `lowerOrthant_ge_iid_of_posDef`.
- Integrated the four modules into the public root, added six permanent theorem audits, completed
  the WP16–WP18 cards, updated repository status, and started two disjoint WP19 scratch
  investigations for matrix/boundary and Gaussian-limit obligations.

**Decisions:**

- Orient `multivariateGaussian_eq_stdGaussian_withDensity` with the multivariate Gaussian on the
  left so its statement matches its name; use symmetry at the deterministic density seam.
- Keep the deterministic box inequality public because it is the explicit cross-module seam from
  the density file to the lower-bound file. Keep specialized scalar CLT-box convergence private.
- Expose only the final centered-product and positive-definite orthant theorems from WP17 and WP18;
  all normalization, asymptotic, and integration helpers remain private.
- Handle singular covariance only in WP19 through outer regularization and an explicit
  coordinate-marginal frontier-null proof.
- Make no commit, as required by project policy.

**Validation:**

- Direct and named warning-as-error gates pass for all four new production modules; the public root,
  permanent audit, and full `lake build --wfail` pass with 3,223 jobs.
- The permanent audit now contains 55 active declarations; every result reports only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scan exits zero. Placeholder, project
  axiom, unsafe, `native_decide`, forbidden-import, width, and `git diff --check` gates are clean.
- Production hashes are `c0a1f5e6…faa4` for `Gaussian/DensityRatio.lean`,
  `dc88f66d…dfd1` for `Product/PositiveLowerBound.lean`, `4a078f35…c4e` for
  `Product/CenteredProduct.lean`, and `18221504…3666` for
  `Orthant/PositiveDefinite.lean`.
- Independent reviews found no WP16 or M2 defect. They checked the density formula and orientation,
  compact minimum, Portmanteau boundary, fixed coefficient, ENNReal/real bridges, normalization,
  deficit direction, dyadic exponent algebra, contradiction, denormalization, and `m = 0`.

**Open Items:**

- [x] Complete and combine WP19 matrix/boundary and Gaussian-coupling reconnaissance.
- [x] Freeze WP19 only after the exact weak-convergence and frontier-null interfaces compile.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Freezing the density identity and deterministic box inequality separately made the lower-bound
uniformity easy to audit, while the minimal WP17 surface kept the M2 contradiction independent of
all CLT and density plumbing. The stale-root audit failure was an integration-order artifact, not a
proof defect; rebuilding the public target before the permanent audit should remain a standard
gate. For WP19, the key risk is the exact coupled multivariate Gaussian law, so matrix admissibility
and frontier nullity should stay independent until that law compiles.

### Close Singular Covariance and M3

**Goal:** Complete WP19, certify the all-covariance lower-orthant theorem, and start the maxima/MGF
reduction.

**Changes:**

- Added `Orthant/Singular.lean`, proving the exact law and weak convergence of the Gaussian
  regularization, positive-definite weak-simplex admissibility of the regularized covariance, and
  lower-orthant frontier nullity from explicit coordinate marginals.
- Passed the WP18 lower-orthant inequality through the continuity-set Portmanteau limit to prove
  `lowerOrthant_ge_iid` for every admissible covariance, closing M3.
- Integrated the singular module into the public root and permanent axiom audit, completed the
  WP19 card, updated the repository status, and opened WP20 coordinate-maxima and MGF
  reconnaissance.

**Decisions:**

- Keep the Gaussian coupling, covariance algebra, centered identity PSD proof, regularization,
  frontier containment, and boundary-nullity results private; expose only the frozen outer
  theorem.
- Use the direct independent-Gaussian coupling with
  `ε n = (1 / 2 : ℝ) ^ (n + 1)` rather than matrix-square-root continuity.
- Handle singular covariance only here; do not extend `centered_product_of_posDef` to singular
  covariance.
- Make no commit, as required by project policy.

**Validation:**

- `Orthant/Singular.lean` has 404 lines and SHA256
  `a373255d…f4306`; its direct warning-as-error and named 3,223-job target gates pass.
- The public root and full warning-as-error build pass with 3,226 jobs.
- The permanent audit now contains 56 active declarations, all reporting only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scanner exits zero. Placeholder,
  project-axiom, unsafe, `native_decide`, forbidden-import, width, whitespace, and diff checks are
  clean.
- Independent review found no defect in the covariance law, convergence, PSD/admissibility,
  frontier-nullity, Portmanteau, or theorem-quantifier arguments.

**Open Items:**

- [x] Freeze and productionize the WP20 coordinate-maximum, tail-order, and exponential-moment
  interfaces.
- [x] Proceed to WP21 codebook normalization after the MGF theorem compiles.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Separating matrix/boundary reconnaissance from two independent Gaussian-law prototypes exposed
the only delicate seams before production. The final one-theorem module is longer than the outer
argument because it proves the coupled law rather than assuming covariance continuity, but its
private surface and explicit marginal proof make the trust boundary auditable. WP20 can now use
strict maximum-tail order without revisiting singular Gaussian analysis.

### Close Coordinate Maxima and Exponential Moments

**Goal:** Complete WP20 and advance from the covariance theorem to the normalized Gaussian MGF
comparison.

**Changes:**

- Added `Maxima/CoordinateMax.lean` with the nonempty finite coordinate maximum, continuity, and
  its exact lower-orthant sublevel characterization.
- Added `Maxima/StochasticOrder.lean`, converting M3 into strict upper-tail domination against the
  identity-covariance Gaussian maximum.
- Added `Maxima/ExponentialMoments.lean`, proving exponential integrability and transferring the
  tail order through ENNReal layer cake to `gaussianMax_mgf_le_regularSimplex`.
- Integrated the three modules into the public root and permanent audit, completed the WP20 card,
  updated repository status, and opened two WP21 normalization reconnaissance tracks.

**Decisions:**

- Keep the basic coordinate module independent of Gaussian analysis; isolate the singular theorem
  dependency in `StochasticOrder.lean`.
- Expose continuity and the sublevel equivalence as stable coordinate interfaces, and expose only
  the strict-tail comparison from the stochastic-order module.
- Establish both exponential-max integrability facts before converting the ENNReal comparison to
  real Bochner integrals; do not use the nonintegrable-integral-zero convention.
- Handle `μ = 0` separately by probability normalization and use `log t / μ` only for `μ > 0`.
- Make no commit, as required by project policy.

**Validation:**

- Production hashes are `1ca42197…f3c516` for `CoordinateMax.lean`,
  `0a9cdb34…c82e` for `StochasticOrder.lean`, and `f7237a8a…a697c5` for
  `ExponentialMoments.lean`.
- All direct and named warning-as-error gates pass; the public root and full build pass with 3,229
  jobs.
- The permanent audit now contains 61 active declarations, all reporting only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scanner exits zero. Placeholder,
  project-axiom, unsafe, `native_decide`, forbidden-import, width, whitespace, and diff checks are
  clean.
- Independent review found no defect in the maximum, product-orthant, tail, integrability,
  layer-cake, logarithm, zero-parameter, or ENNReal/Bochner arguments.

**Open Items:**

- [x] Compile and freeze the WP21 normalized-covariance and common-Gaussian MGF identities.
- [x] Prove the Gram-matrix comparison against the regular-simplex Gram matrix.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

The three-file split keeps the elementary maximum reusable and makes the only dependence on the
singular covariance theorem visible. Proving integrability through coordinate marginals avoided a
semantic trap in mathlib's total Bochner integral, while strict-tail layer cake matched WP19's
complement direction without requiring a general stochastic-order library. WP21 can now focus
entirely on scaling and common-shift algebra.

### Close Gram Normalization and the Gram-Matrix MGF Comparison

**Goal:** Complete WP21 by transferring the normalized-covariance MGF comparison back to arbitrary
correlation matrices and identifying the regular-simplex target.

**Changes:**

- Added `Coding/Gram.lean` with the regular-simplex Gram matrix and the bundled matrix
  normalization facts.
- Added `Coding/Normalization.lean` with the exact independent common-Gaussian law, maximum
  identities, exponential integrability, witnessed product factorization, and prefactored-MGF
  identity.
- Added `Coding/RegularSimplex.lean`, applying WP20 at the normalized covariance and cancelling the
  common positive Gaussian prefactor to prove `gramGaussianMax_mgf_le_regularSimplex`.
- Integrated the three modules into the public root and permanent axiom audit, completed the WP21
  card, and updated repository status.

**Decisions:**

- Keep the matrix-first architecture: WP21 accepts an arbitrary correlation matrix and introduces
  no codebook realization.
- Retain the mathematical source's exact coupling
  `sqrt ((m-1)/m) • (ξ + B • 1)` with `Var(B)=1/(m-1)`; keep the independently proved
  `N(0,1/m)` scaled-shift construction only as reconnaissance evidence.
- Expose only the regular-simplex target, one inline matrix bundle, one inline MGF normalization
  identity, and the acceptance theorem; keep every implementation helper private.
- Consume the explicit product-integrability witness through Fubini before factoring the MGF.
- Make no commit, as required by project policy.

**Validation:**

- Production hashes are `5db85554…cd8db7` for `Gram.lean`,
  `b536fe1a…d71173` for `Normalization.lean`, and `502f1a19…ab933a` for
  `RegularSimplex.lean`.
- All direct and named warning-as-error gates pass; the public root and full build pass with 3,232
  jobs.
- The permanent audit contains 65 active declarations, all reporting only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scanner exits zero. Placeholder,
  project-axiom, unsafe, `native_decide`, forbidden-import, width, whitespace, and diff checks are
  clean.
- Independent cross-review found no defect in the matrix algebra, Gaussian law, covariance
  scaling, maximum identities, integrability, product factorization, prefactor identity, WP20
  application, or positive cancellation.

**Open Items:**

- [x] Proceed to WP22 and close the codebook/Bayes-value reduction.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Freezing the outer theorem only after two independent Gaussian-law constructions prevented the
common variance and scaling coefficients from becoming implicit trust assumptions. The final split
keeps the 680-line implementation behind three compact seams, while the acceptance proof itself is
only the normalization sandwich and a positive-factor cancellation. The remaining theorem is now
geometric and decision-theoretic rather than analytic.

### Close the Bayes/ML Identity and Operational Weak Simplex Theorem

**Goal:** Complete WP22 and close M4 with a tie-safe operational classification theorem.

**Changes:**

- Added `Coding/BayesValue.lean`, defining the code Gram matrix, exact shifted-Gaussian likelihood
  ratios, their pointwise finite maximum, and the uniform-prior Bayes value. The module proves the
  Gaussian score pushforward, explicit integrability, and `bayesValue_eq_gramMgf`.
- Added `Coding/MLDecoder.lean`, constructing the measurable least-index likelihood maximizer,
  proving its maximizing and tie-breaking properties, and identifying its average success under
  the shifted class measures with the Bayes value.
- Added `Coding/WeakSimplex.lean`, identifying every supplied regular-simplex realization with the
  matrix target, proving an arbitrary-`m` Bayes comparison across different ambient dimensions,
  and closing the operational theorem `weak_simplex` for `n + 1` signals in `Coord n`.
- Integrated all three modules into the public root and permanent axiom audit, completed the WP22
  card, and updated the repository status.

**Decisions:**

- Define the Bayes value at the pointwise density-maximum boundary before constructing a decoder;
  do not define it as an MGF.
- Resolve all ties by the least `Fin` index using `Fin.find`; keep the generic selector and density
  partition private while exporting the specialized measurable ML interface.
- State `weak_simplex` in operational success-probability form. Retain the stronger arbitrary-`m`
  Bayes comparison as public support and derive simplex unit norms from exact Gram equality.
- Quantify over a supplied regular-simplex realization. Do not claim a canonical realization in
  `Coord n` without a separate hyperplane-isometry or Gram-realization theorem.
- Include `lam = 0`; the proof needs only nonnegativity and the tie-heavy endpoint is valid.
- Make no commit, as required by project policy.

**Validation:**

- Production hashes are `811e4998…0f7813` for `BayesValue.lean`,
  `790f4581…74d835` for `MLDecoder.lean`, and `da3f0a18…4592c` for
  `WeakSimplex.lean`.
- All direct and named warning-as-error gates pass; the public root and full build pass with 3,235
  jobs.
- The permanent audit contains 89 active declarations, including all 24 WP22 exports, and every
  entry reports only `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scanner exits zero. Placeholder,
  project-axiom, unsafe, `native_decide`, forbidden-import, width, whitespace, and diff checks are
  clean.
- Three independent cross-reviews found no defect in the covariance orientation, likelihood
  normalization, finite maximum, integrability, selector/tie partition, shifted class measures,
  regular-simplex reduction, comparison direction, or final quantifiers.

**Open Items:**

- [x] Proceed to WP23 publication preparation and the repository-wide theorem/provenance audit.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Compiling the score, selector, and final reductions through actual temporary imports exposed the
only cross-module requirements before production. The resulting endpoint is operational rather
than merely matrix-valued, while the generic selector and analytic implementation remain private.
The one explicit scope boundary is geometric realization: the theorem covers every supplied
regular simplex but does not manufacture canonical coordinates.

### Advance WP23 to the Publication Boundary

**Goal:** Complete every local M5 audit and identify the exact decisions that require repository
owner authorization.

**Changes:**

- Converted `Audit/Axioms.lean` into the permanent active dossier and added direct entries for the
  two vendored StatLean helper tops that were previously covered only transitively.
- Reconciled all five vendored Lean files with `PROVENANCE.md`, including upstream paths and
  commits, whole-file or slice hashes, local diffs, import closure, license copy, and axiom result.
- Added the public theorem dependency map and exact clean-checkout reproduction commands to the
  README, and corrected stale WP00/WP22 audit-status wording.
- Repaired the trusted-source scanner so production rejects `native_decide` and both root-level and
  project-qualified Scratch/Scaffold imports. Independent adversarial passes also closed
  interpolation/literal desynchronization, escaped-identifier, raw-reparse, numeric/character
  token-fusion, and absolute-path classification gaps.

**Decisions:**

- Treat a source-only snapshot with no `.git`, `.lake`, `.olean`, or `.ilean` as strong
  cache-independent reproduction evidence, but do not call it a literal clean clone.
- Keep normal, raw, and escaped-identifier text visible to the forbidden-token scan, treat character
  literals atomically, and mask only comment text recognized by both structural views. This
  deliberately prefers bounded false positives over another fail-open literal/parser heuristic.
- Do not select a project license or make a coherent repository commit without owner authorization.
- Classify hosted-CI observation and the tracked machine-specific Codex configuration as release
  hygiene, not blockers to the Lean build or trust result.
- Make no commit, as required by project policy.

**Validation:**

- A fresh 162-file source snapshot resolved mathlib to
  `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`, fetched 8,542 binary-cache files, and passed the
  3,235-job warning-as-error build, public-root check, and permanent axiom audit.
- The permanent dossier has 91 active declarations; every entry reports exactly
  `[propext, Classical.choice, Quot.sound]`.
- All 47 production modules are reachable from the public root, with no Scratch/Scaffold node and
  no tracked compiled artifact.
- The repaired scanner's 30 tests and the official production scan pass. Compiling probes cover
  explicit and bare interpolation, nested/custom delimiters, raw-term reparsing, escaped
  identifiers, numeric and character adjacency, and production/nonproduction path boundaries.
  Two independent bounded rereviews found no remaining defect in the final slice.

**Open Items:**

- [x] Obtain the owner's project-license decision.
- [x] Obtain authorization for a coherent commit.
- [ ] Verify a literal clean clone and hosted CI.

**Self-Reflection:**

The source-only snapshot separates reproducibility of the formalization from the repository's
uncommitted state, while the synthetic scanner probe turned a clean-but-incomplete audit into a
regression-backed trust check. The remaining M5 boundary is governance rather than mathematics:
the theorem, proof graph, provenance, and local build are audited, but publication metadata and a
cloneable revision require explicit owner action.

### License and Prepare the Release Commit

**Goal:** Apply the owner-selected MIT license and prepare the audited formalization as a coherent release commit.

**Changes:**

- Added the standard MIT license with the authenticated repository-owner identity `abhmul`, declared `license = "MIT"` in Lake metadata, and documented the project license in the README.
- Clarified that the five vendored StatLean Lean files and copied upstream license remain Apache-2.0 and are not relicensed by the project MIT license.
- Added the machine-specific `.codex/config.toml` to `.gitignore` and removed it from tracking while preserving the local file and sanitized tracked example.
- Created the release branch `agent/publish-formalization`; production sources, audit/provenance, package records, CI, and publication metadata form the release scope, while unaudited `Scratch/` prototypes remain local and uncommitted.

**Decisions:**

- Do not attach this checkout to the similarly named private repository discovered under the authenticated account: it has unrelated history.
- Do not create a new GitHub repository without explicit authorization; complete the local commit and literal clean-clone gate first.

**Validation:**

- `lake build --wfail` passes all 3,235 jobs; the public root and permanent axiom dossier compile warning-as-error.
- All 91 audited declarations report exactly `[propext, Classical.choice, Quot.sound]`.
- The 30 trusted-source scanner tests, official production scan, six-artifact provenance check, Lean width check, and staged-diff check pass.

**Open Items:**

- [x] Commit the coherent release slice and verify it from a literal clean clone.
- [ ] Configure a safe GitHub remote, push the branch, and observe hosted CI.

**Self-Reflection:**

Separating the release surface from unaudited prototype copies keeps the published trust boundary precise. Checking repository identity before adding a remote also prevented an unrelated project from receiving a history with no common ancestor.

### Close M5 from a Literal Clean Clone

**Goal:** Verify the authorized coherent release commit without source-copy or build-artifact reuse and close M5.

**Changes:**

- Committed the 87-file formalization and publication slice as `7d48b070c9a474de78bae2e47d458861b30d940a` on `agent/publish-formalization`.
- Cloned that branch with `git clone --no-local --single-branch` into a fresh directory containing no `.lake`, `.olean`, or `.ilean` artifacts.
- Used a separate initially empty XDG cache on the workspace filesystem; an initial `/tmp` attempt was discarded after tmpfs quota prevented 197 of 8,542 cache decompressions.

**Decisions:**

- Close M5 on the successful workspace-filesystem clone: the failed tmpfs attempt was an environmental storage limit, while the identical commit and commands passed with sufficient space.
- Keep branch publication separate from formalization acceptance because no configured or existing GitHub repository has matching history; do not create a new repository without explicit authorization.

**Validation:**

- `lake update` checked out mathlib exactly at `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f` and fetched and decompressed all 8,542 cache artifacts.
- `lake build --wfail` passed all 3,235 jobs; direct public-root and permanent-audit compilation passed warning-as-error.
- All 91 axiom entries were exactly `[propext, Classical.choice, Quot.sound]`; scanner tests passed 30/30, the official scan and six-artifact provenance gate passed, and the 48-node import graph contained all 47 production modules plus the root with no Scratch/Scaffold node.
- The tested clone remained Git-clean at the exact release commit.

**Open Items:**

- [ ] Configure or create a safe GitHub remote, push `agent/publish-formalization`, and observe hosted CI.

**Self-Reflection:**

Testing an actual commit rather than another source copy closed the final reproducibility ambiguity. Keeping the dependency cache isolated exposed the tmpfs capacity assumption; rerunning unchanged on a filesystem with adequate space distinguished infrastructure failure from a project defect.
