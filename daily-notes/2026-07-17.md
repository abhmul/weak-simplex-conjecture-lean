---
aliases:
title: 2026-07-17
tags:
  - daily-note
  - ai-generated
---
# 2026-07-17
[[2026-07-16]]<- [[2026-07-17]] -> [[2026-07-18]]

## Session Notes

### Set Up Lean Agent Tooling

**Goal:** Configure repository-local Lean agent tooling and remove the obsolete handoff archive.

**Changes:**

- Fixed the required Lean LSP MCP configuration, pinned `lean-lsp-mcp` to `0.28.1`, and trusted the checkout in the user-level Codex configuration.
- Pulled the pinned Lean workflow source, copied its core skill and references into `.agents`, and added repository-local Claude discovery links.
- Updated governing documents and synchronized report copies to reference the repository-local skills.
- Moved `weak-simplex-codex-handoff.zip` to the desktop trash.

**Decisions:**

- Keep the copied Lean skill at the report's pinned commit and omit upstream plugin hooks, autonomous commands, helper runtime, and subagents.
- Use absolute MCP executable, project, and working-directory paths because Codex does not expand `~` in `cwd`.

**Validation:**

- A fresh Codex session loaded `$lean4` from `.agents/skills/lean4/SKILL.md`.
- A fresh Codex session successfully ran `lean_diagnostic_messages` through the project MCP configuration.
- The copied skill matched the pinned source snapshot, both Claude links resolved, and `lake build` passed.

**Open Items:**

- [ ] Review and checkpoint the pre-existing Lean configuration, templates, and v3 mathematical source separately.

**Self-Reflection:**

Separating the direct MCP protocol probe from fresh Codex-session probes isolated server health, path expansion, and project trust cleanly. Future setup work should test those layers in that order.

### Close WP00 Foundation

**Goal:** Complete the reproducible baseline, CI trust gates, provenance ledger, and axiom-audit skeleton required before theorem work.

**Changes:**

- Instantiated a frozen WP00 card with explicit ownership, allowed imports, acceptance deliverable, and stop budget.
- Replaced the template `hello` declaration with a theorem-free `WeakSimplex` namespace and documented public root.
- Added the milestone axiom-audit skeleton, vendor policy README, and repository provenance ledger.
- Hardened CI with warning-as-error builds, separate public/audit root compilation, import-graph generation, a tested Lean lexical trust scanner, forbidden-import checks, and complete vendor-entry checks.
- Replaced the generated repository README with project status and reproduction instructions.

**Decisions:**

- Keep WP00 configuration-only and leave M0 open until WP01–WP03 freeze and compile real theorem interfaces.
- Permit audited project wrappers to import `Vendor`, forbid the public root from importing `Vendor` directly, and forbid every production module from importing `Scratch` or `Scaffold`.
- Defer empty branch modules and speculative theorem headers to their owning work packages.
- Do not commit autonomously, per project policy.

**Validation:**

- Lean 4.31.0 and Lake 5.0.0 matched the pinned environment; the mathlib checkout matched `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`.
- `lake env lean` passed for `Basic.lean`, the public root, and `Audit/Axioms.lean`; `lake build --wfail` passed.
- The public-root import graph generated successfully, workflow YAML parsed, trust-scanner unit tests passed, and all local trust/import/provenance gates passed.
- Two independent read-only reviews were run; their import-modifier, declaration-modifier, vendor-policy, provenance, and card-fidelity findings were addressed.

**Open Items:**

- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.
- [ ] Instantiate disjoint Wave A cards for WP01, WP02, and WP03; M0 remains open pending compiled theorem interfaces.
- [ ] Locate or add the associated-paper Markdown source named by the reassessment report before a package requires it.

**Self-Reflection:**

The first CI regexes mirrored the report literally but missed valid Lean forms such as modifier-prefixed and same-line-comment declarations. Replacing them with a tested lexical scanner was more reliable; future trust-gate work should begin with adversarial syntax probes against the pinned parser, not only positive-path execution.

### Advance Through Adaptive Witnesses and Prékopa

**Goal:** Execute the reassessment plan through the next independently verifiable adaptive- and product-branch milestones without weakening statements or expanding the trust boundary.

**Changes:**

- Completed WP01's nine-file API spike and audit, selecting the one-file StatLean Prékopa source, a 397-line selective Gaussian-shift closure, compact-sphere coercivity, and an explicit positive-definite Gaussian density ratio.
- Completed WP02–WP05: finite/Euclidean/matrix interfaces, lower-orthant measurability, normal CDF and truncated moments, Mills bounds and tilt calculus, quadratic coercivity, inverse action, and the rank-one inverse bound.
- Completed WP06 and WP07: the unconstrained adaptive potential, compact negative superlevels, an assumption-minimal global maximizer, coordinate-line stationarity, compatibility, the value identity, and `AdaptiveWitnesses`.
- Completed WP10: vendored exactly one attributed StatLean Prékopa source plus its Apache-2.0 license, then added strict-weight ENNReal log-concavity, convex indicators, positive-semidefinite Gaussian kernels, measurable marginalization, and convolution closure.
- Added every completed package to the public root and permanent axiom audit, updated package cards and README status, and recorded exact vendor hashes and changes in `PROVENANCE.md`.
- Disabled only the mathlib copyright-header style linter because the repository declares no license; the mathlib standard linter set remains enabled.

**Decisions:**

- Keep the hR-only maximizer theorem separate from its rank-one benchmark corollary so dimension and semidefinite assumptions remain minimal.
- Use the scalar finite-sum coordinate-line derivative after the direct PiLp-valued route exposed an instance diamond; keep every row/qform helper private.
- Preserve the report's positive-semidefinite Gaussian-kernel theorem; its exact Jensen remainder proof closed without strengthening the covariance assumption.
- Share future StatLean density support between WP08 and WP11 rather than vendor duplicate `PiWithDensity` copies; both reconnaissance tasks are active.
- Make no commit, as required by the project coordination policy.

**Validation:**

- Lean 4.31.0 and Lake 5.0.0 match the pinned toolchain; `lake build --wfail` passes with 3,085 jobs after WP07/WP10 integration.
- Direct source gates, warning-as-error package targets, public-root compilation, and the permanent audit pass. All sixteen current audit entries report only `[propext, Classical.choice, Quot.sound]`.
- The trusted-source scanner's 10 unit tests pass and its production scan exits zero. Width, whitespace, placeholder, project axiom/constant, unsafe, `native_decide`, broad-import, Scratch/Scaffold-import, and `git diff --check` gates are clean.
- StatLean hashes match the frozen upstream source (`ecf65de…2538`), local namespaced source (`aba9c34c…dd04`), and exact license (`d5945fe0…cd93`); the CI provenance gate passes.
- Independent read-only reviews found no mathematical or interface defect in WP06, WP07, or WP10 after one scratch/public naming claim was corrected in the WP07 card.

**Open Items:**

- [ ] Complete the active WP08 Gaussian-shift/shared-vendor reconnaissance and freeze its smallest compiled interface.
- [ ] Complete the active WP11 symmetric-rectangle reconnaissance, including the product-density, monotone-covariance, and regression-coercion blockers.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Compiling whole interfaces in `/tmp` before freezing production statements made the high-risk PSD-kernel and coordinate-derivative proofs tractable. The cold-build failure between two new dependent modules also showed that direct source checks and Lake object installation are distinct gates; future cards should encode that ordering from the outset. Independent review was most useful when it checked mathematical directionality and literal card claims, not merely whether Lean accepted the term.

### Close Gaussian Tilting, Conditional M1, and Symmetric Rectangles

**Goal:** Complete the next adaptive/product interface packages and push the product branch through
the positive-definite symmetric Gaussian rectangle theorem.

**Changes:**

- Completed WP08 with the exact 397-line StatLean Gaussian-shift closure, project PSD measure and
  lintegral wrappers, centered tilted half-line regularity/mass/zero-barycenter identities, and the
  adaptive endpoint preimage theorem.
- Completed WP09 with the cycle-free `CenteredProductProperty` interface and the conditional
  positive-definite lower-orthant theorem. The final unparameterized theorem name remains reserved
  for WP17/WP18 integration.
- Completed WP11 with the exact 62-line StatLean Pi-Gaussian provider and a direct regression,
  Prékopa, threshold, and `Fin`-induction proof of the positive-definite symmetric rectangle
  inequality.
- Added the new public modules, vendor provenance and README entries, and permanent axiom audits.
  Updated package cards and repository status.

**Decisions:**

- Keep measurability explicit in `CenteredProductProperty` because the project's pointwise ENNReal
  log-concavity predicate intentionally carries no measurable structure.
- Name the temporary result `lowerOrthant_ge_iid_of_posDef_of_centeredProduct`; do not freeze the
  report's final theorem with an extra dependency parameter.
- Represent the regression residual as a linear image of a full product Gaussian. Its covariance can
  be singular, so normalization, inversion, and a residual density are neither valid nor needed.
- Port only the exact 62-line Pi-Gaussian provider; reject the 961-line Anderson development and its
  broad dependency closure.
- Make no commit, as required by project policy.

**Validation:**

- All WP08, WP09, and WP11 direct files and package targets pass with warnings as errors. The public
  root, permanent audit, and full `lake build --wfail` pass with 3,111 jobs.
- The permanent audit now contains 27 entries; every entry reports only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests and the production scan pass. Width, whitespace, placeholder,
  project axiom/constant, unsafe, `native_decide`, Scratch/Scaffold import, vendor coverage/hash, and
  `git diff --check` gates are clean.
- Exact local vendor hashes are `aa812dce…76d3`, `b2728647…dcf4`, `c9dd5eca…7448`,
  `5c906062…b890`, `aba9c34c…dd04`, and license `d5945fe0…cd93`.
- Independent read-only reviews found no mathematical, interface, vendor, trust, or integration
  defect in WP08, WP09, or WP11. They separately checked all inequality, shift, Fubini, threshold,
  and induction directions.

**Open Items:**

- [ ] Complete the active WP12 even-factor layer-cake reconnaissance and freeze a minimal interface.
- [ ] Complete the active WP13 normalized self-convolution reconnaissance and identify its smallest
  law-identity blocker.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Freezing temporary dependency propositions under distinct theorem names prevented a conditional
milestone from corrupting the final public API. The regression proof also confirmed that choosing a
full standard-Gaussian source is materially cheaper and more robust than manufacturing a density for
a possibly singular derived covariance. Future product packages should continue to prove law
identities before recomputing moment integrals.

### Complete Even Factors and Normalized Self-Convolution

**Goal:** Close the next two product-branch interfaces and begin the sum--difference package without
changing the centered-product architecture or trust boundary.

**Changes:**

- Completed WP12 with a local strict-superlevel layer-cake proof of the bounded even log-concave
  Gaussian product inequality. The proof classifies convex symmetric subsets of `ℝ` up to Gaussian
  endpoint nullity, approximates unbounded coordinates by finite rectangles, and uses an explicit
  finite-level Tonelli identity.
- Completed WP13 with the analytic normalized self-convolution transform, its exact
  normalized-average measure identity, and law-derived preservation of mass one, zero Gaussian
  barycenter, and variance.
- Added both modules to the public root and all nine new theorem audits to the permanent audit file.
  Updated the work-package cards and repository status.
- Started two disjoint scratch investigations for WP14: the multivariate Gaussian sum--difference
  rotation law and the deficit-inequality assembly.

**Decisions:**

- Reject StatLean `Anderson.lean`: its only layer-cake lemma is scalar and cannot express a vector of
  coordinate superlevels; the full transitive closure is 4,012 lines.
- Keep every WP12 classifier, approximation, set inequality, and Tonelli helper private. The only
  public declaration is `even_logConcave_product_of_posDef`.
- Treat the WP13 map equality as an equality of finite density measures; it becomes a probability
  law after the mass-one theorem. Do not claim preservation of a general mean, which scales by
  `√2`; expose only the zero-barycenter consequence needed downstream.
- Record the measured WP13 dependency on WP10 only. WP12 first enters the WP14 deficit proof, so the
  two packages can be implemented in parallel without changing theorem architecture.
- Make no commit, as required by project policy.

**Validation:**

- WP12 and WP13 direct and target warning-as-error gates pass. The public root and permanent audit
  compile, and `lake build --wfail` passes with 3,113 jobs.
- The permanent audit now has 36 active declarations; every result reports only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scan exits zero. Width, whitespace,
  placeholder, project axiom/constant, unsafe, `native_decide`, forbidden import, and
  `git diff --check` gates are clean.
- Final production hashes are `ab78a816…c48f5` for `Product/EvenLayerCake.lean` and
  `c8016f63…4090f` for `Product/NormalizedSelfConvolution.lean`.
- Independent read-only reviews found no mathematical, interface, trust, import, or edge-case defect.
  They separately checked `m = 0`, interval endpoint handling, all Tonelli and inequality directions,
  Gaussian-density normalization, rotation orientation, and every moment derivation.

**Open Items:**

- [ ] Complete WP14 sum--difference reconnaissance and freeze the smallest reusable theorem.
- [ ] Proceed to WP15 only after the WP14 deficit inequality is production-complete.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Splitting WP12 into an independently compiled coordinate-set theorem and a separate level-vector
Tonelli identity exposed that the external scalar Anderson lemma was structurally mismatched, not
merely inconvenient. For WP13, proving the measure law before moments eliminated duplicated
integral algebra and caught the important distinction between zero-barycenter preservation and
general-mean scaling. The same law-first discipline should guide WP14 and WP15.

### Complete the Sum–Difference Deficit Theorem

**Goal:** Close WP14 with a reusable joint Gaussian rotation boundary and the exact one-step
normalized self-convolution deficit inequality.

**Changes:**

- Added `Product/SumDifference.lean` with the joint normalized sum–difference product law for two
  centered multivariate Gaussian copies.
- Proved `normalizedSelfConvolution_product_deficit_of_posDef`, the real inequality
  `Z_R(Dh) ≤ Z_R(h)^2`, using the WP12 even-factor theorem, the WP13 transform, product Fubini,
  and Gaussian rotation invariance.
- Added the module to the public root and both declarations to the permanent axiom audit. Updated
  WP14 and the repository status.
- Started disjoint read-only reconnaissance for WP15's dyadic law and central-limit interfaces.

**Decisions:**

- Use mathlib's stronger `IsGaussian.map_rotation_eq_self` boundary instead of rebuilding joint
  Gaussianity, cross-covariance zero, and independence componentwise.
- Keep mass one, zero barycenter, and variance assumptions out of WP14; the one-step deficit theorem
  does not consume them.
- Expose the joint map equality as the reusable independence boundary and keep all coefficient,
  fixed-average, integrability, and Fubini helpers private.
- Make no commit, as required by project policy.

**Validation:**

- The 374-line production file has SHA-256
  `17455adb366705016e4a0bfbf561bbb2604c5bafe426efe053ac359203b832c4`. Direct and target
  warning-as-error gates pass.
- The public root, permanent audit, and full `lake build --wfail` pass with 3,114 jobs. The audit
  now contains 38 active declarations, each using only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scan exits zero. Width, placeholder,
  project axiom, unsafe, `native_decide`, and `git diff --check` gates are clean.
- Independent review verified the deficit direction, rotation signs, Fubini and integrability
  hypotheses, and the `m = 0` case without finding a defect.

**Open Items:**

- [ ] Freeze WP15 only after its dyadic-law and CLT reconnaissance identify a proof-complete public
  boundary.
- [ ] Complete the explicit positive-definite density-ratio dependency before WP16.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

The direct Gaussian rotation theorem collapsed a potentially brittle covariance-and-independence
subproof into a stronger joint-law equality already present in the pinned checkout. Separating the
fixed-average inequality from the outer Fubini identity made the inequality direction independently
reviewable and kept the zero-dimensional case automatic. WP15 should likewise separate finite
dyadic law algebra from asymptotic CLT convergence before freezing its acceptance theorem.

### Complete the Dyadic Law and Central Limit Interface

**Goal:** Close WP15 with exact iterated normalized-self-convolution laws, nondegenerate variance,
and convergence of the actual iterated density measures.

**Changes:**

- Added `Product/DyadicCLT.lean` with the recursive transform and propagation of measurability,
  nonnegativity, boundedness, log-concavity, mass one, zero Gaussian barycenter, and variance.
- Proved that the depth-`r` transform is the law of the normalized sum of exactly `2 ^ r` iid
  copies, including the exact `1 / sqrt (2 ^ r)` coefficient.
- Proved strict positivity of the initial tilted law's variance from Gaussian non-atomicity and
  transferred mathlib's one-dimensional CLT along the dyadic subsequence to the varying iterated
  density measures.
- Added the module to the public root and eleven declarations to the permanent audit. Updated WP15
  and the repository status.

**Decisions:**

- Use `Measure.infinitePi` for the canonical `ℝ^ℕ` probability space; this is the pinned API
  corresponding to the report's informal `Measure.pi` notation.
- Expose only the recursive factor, its analytic/moment invariants, the exact iid law, strict
  variance positivity, and direct convergence. Keep balanced convolution powers, characteristic
  functions, and product-space plumbing private.
- Derive every iterated probability instance from mass preservation in the acceptance theorem; do
  not add a probability-instance hypothesis.
- Make no commit, as required by project policy.

**Validation:**

- The 479-line production file has SHA-256
  `efe8fa9e8ce7b1a2fabdf82f8e39cf3fa54f1b7f30b506248275af0a572de314`. Direct and target
  warning-as-error gates pass.
- The public root, permanent audit, and full `lake build --wfail` pass with 3,219 jobs. The audit
  now contains 49 active declarations, each using only
  `[propext, Classical.choice, Quot.sound]`.
- Trusted-source scanner tests pass 10/10 and the production scan exits zero. Width, placeholder,
  project axiom, unsafe, `native_decide`, forbidden-import, and `git diff --check` gates are clean.
- Independent production review verified dyadic indexing, normalization, law directions, moment
  hypotheses, the non-atomic variance proof, varying-source convergence, the `r = 0` case, and the
  literal card/integration boundary.

**Open Items:**

- [ ] Complete WP16 density-ratio and compact-interval probability reconnaissance before freezing
  its public boundary.
- [ ] Combine WP14 and WP15 with WP16's lower bound in WP17; do not claim M2 early.
- [ ] Observe the first hosted GitHub Actions run after an authorized commit and push.

**Self-Reflection:**

Separating the exact finite law from the asymptotic CLT made every normalization coefficient
auditable and exposed the one real interface wrinkle: `TendstoInDistribution` needs probability
instances for its varying source measures. Deriving those instances from iterated mass preservation
kept the acceptance theorem assumption-minimal. The same boundary-first approach should keep WP16's
density formula, compact minimum, and scalar interval limits independently testable.
