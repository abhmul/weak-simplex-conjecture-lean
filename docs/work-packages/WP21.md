# WP21 — Gram normalization and the regular simplex

- **ID:** WP21
- **Status:** complete
- **Reconnaissance owners:** `/root/wp21_matrix` for covariance and regular-simplex matrix algebra;
  `/root/wp20_coordinate` for the common-Gaussian law and MGF identity
- **Integration owner:** `/root`
- **Owned production files after interface freeze:**
  `WeakSimplexConjectureLean/Coding/Gram.lean`,
  `WeakSimplexConjectureLean/Coding/Normalization.lean`, and
  `WeakSimplexConjectureLean/Coding/RegularSimplex.lean`
- **Integration-owned files:** this card, the public root, `Audit/Axioms.lean`, README, and the
  daily note
- **Stop budget:** eight hours total: two for matrix admissibility, three for the common-Gaussian
  law, two for the MGF identity and comparison, and one for review/audit; at most four focused
  compiler/LSP repair cycles per named blocker
- **Upstream acceptance theorem:** `WeakSimplex.gaussianMax_mgf_le_regularSimplex`
- **Acceptance theorem:** `WeakSimplex.gramGaussianMax_mgf_le_regularSimplex`

## Mathematical role

Let `G` be an `m × m` correlation matrix and set

```text
α = (m - 1) / m,
R = α • G + (1 / m) • J.
```

For `m > 1`, `R` is an admissible weak-simplex covariance. If `ξ ∼ N(0,G)` and an independent
`B ∼ N(0,1/(m-1))` is added to every coordinate, then

```text
X = sqrt α • (ξ + B • 1) ∼ N(0,R).
```

Writing `μ = λ / sqrt α`, the common shift gives

```text
exp (-λ²/2) * mgf (max ξ) λ = exp (-μ²/2) * mgf (max X) μ.
```

The regular-simplex Gram matrix is

```text
GΔ = (m / (m - 1)) • (I - J / m),
```

and its normalized covariance is exactly `I`. Applying WP20 to `R`, applying the same normalization
identity to `GΔ`, and cancelling the positive exponential prefactor yields the Gram-matrix MGF
comparison. This package does not define a codebook or Bayes value; those belong to WP22.

Relevant plan sections are §§2.2–2.3, 3.1, 6.3, 10, 11.7, 12 (WP21), 13, and 18. The relevant
mathematical source sections are §§3–4 and 10 of `notes/simplex_optimality_proof.v3.md`.

## Frozen acceptance API

The single acceptance theorem is frozen as:

```lean
namespace WeakSimplex

/-- Every correlation-matrix Gaussian maximum has no larger nonnegative MGF than the
regular-simplex Gaussian maximum. -/
theorem gramGaussianMax_mgf_le_regularSimplex
    {m : ℕ} (hm : 1 < m)
    (G : Matrix (Fin m) (Fin m) ℝ)
    (hG : IsCorrelation G)
    (lam : ℝ) (hlam : 0 ≤ lam) :
    mgf (coordinateMax (Nat.zero_lt_of_lt hm)) (multivariateGaussian 0 G) lam ≤
      mgf (coordinateMax (Nat.zero_lt_of_lt hm))
        (multivariateGaussian 0 (regularSimplexGram m)) lam

end WeakSimplex
```

The WP20 theorem name is intentionally retained for the normalized-covariance layer; this theorem
is the distinct Gram-matrix layer.

## Frozen supporting declaration surface

- `regularSimplexGram`, because WP22 must identify the comparison target;
- `gramNormalization`, the single inline matrix-data bundle used across the `Gram` and
  `RegularSimplex` module boundary;
- `gramMgf_normalization_identity`, the exact inline common-Gaussian identity used across the
  `Normalization` and `RegularSimplex` module boundary;
- `gramGaussianMax_mgf_le_regularSimplex`, the sole acceptance theorem;
- all scale definitions, normalized-covariance definitions, matrix lemmas, product couplings,
  map laws, integrability lemmas, and cancellation helpers remain private.

The exact production imports are frozen provisionally as follows and may only be narrowed:

1. `Coding/Gram.lean`: `Core.Correlation`, `Mathlib.Algebra.Order.Chebyshev`, and
   `Mathlib.Algebra.Order.Star.Real`;
2. `Coding/Normalization.lean`: `Maxima.ExponentialMoments`,
   `Mathlib.Algebra.Order.GroupWithZero.Finset`, and
   `Mathlib.MeasureTheory.Integral.Prod`;
3. `Coding/RegularSimplex.lean`: `Coding.Gram` and `Coding.Normalization`.

## Search and prototype record

Reconnaissance begins with exact pinned-mathlib search for:

- matrix PSD/scalar/add/sub identities and existing Gram-matrix constructors;
- `IsGaussian.ext`, covariance under products and continuous-linear maps, and scalar Gaussian
  marginals;
- `Finset.sup'` under common addition and multiplication by a nonnegative scalar;
- `integral_prod`, `mgf_add`, Gaussian MGF formulas, `MeasurePreserving.integral_comp`, and
  `MeasurePreserving.integrable_comp_of_integrable`;
- exact square-root/division identities needed for `μ = λ / sqrt α`;
- a packaged common-shift or equicorrelation Gaussian law.

Candidate declarations and failed approaches must be recorded before defining a project helper.

## Reconnaissance split

1. `/tmp/WP21Matrix.lean`: compile `regularSimplexGram`, normalized-covariance PSD, unit diagonal,
   weak-simplex admissibility, and normalization of the regular-simplex matrix to identity.
2. `/tmp/WP21Normalization.lean`: compile the product-space common-Gaussian law, pointwise maximum
   under positive scaling and common translation, exponential integrability, and the exact MGF
   normalization identity.
3. The integration owner compiles the exact acceptance theorem in `/tmp/WP21Combined.lean` before
   freezing production interfaces.

## Forbidden shortcuts

- Do not realize a general PSD matrix by choosing codebook vectors or an ambient basis.
- Do not assume the common-Gaussian representation, MGF normalization identity, regular-simplex
  PSD, or normalized identity as production parameters.
- Do not conflate matrix `1` with `allOnesMatrix m`.
- Do not divide by `m - 1` without the explicit `hm : 1 < m` dependency.
- Do not define measurable `argmax`, a decoder, or a Bayes value in this package.
- Do not use the Bochner-integral-zero convention in place of integrability.
- Do not add external code, placeholders, project axioms, unsafe declarations, `native_decide`, or
  broad imports.
- Reconnaissance agents do not edit repository files or commit.

## Acceptance and audit

```bash
lake env lean WeakSimplexConjectureLean/Coding/Gram.lean
lake env lean WeakSimplexConjectureLean/Coding/Normalization.lean
lake env lean WeakSimplexConjectureLean/Coding/RegularSimplex.lean
lake build --wfail WeakSimplexConjectureLean.Coding.RegularSimplex
lake build --wfail
```

The integration owner will add:

```lean
#print axioms WeakSimplex.regularSimplexGram
#print axioms WeakSimplex.gramGaussianMax_mgf_le_regularSimplex
```

Completion additionally requires frozen exact imports/public surface, direct/target/full
warning-as-error gates, trusted-source scans, and independent review of every scaling coefficient,
variance, MGF prefactor, positivity cancellation, and the regular-simplex matrix identity.

## Implementation log

- The matrix-first architecture remains in force: WP21 starts from an arbitrary correlation matrix
  `G` and introduces codebook geometry only in the later Bayes/codebook package.
- Matrix reconnaissance is proof-complete in `/tmp/WP21Matrix.lean` (170 lines; SHA-256
  `90997e50f470024f8e338568747b0a191d3d24ec9313aca7e56ceeeaa53c0ea6`). Its exact imports are
  `Core.Correlation`, `Mathlib.Algebra.Order.Chebyshev`, and
  `Mathlib.Algebra.Order.Star.Real`.
- The matrix prototype proves normalized-covariance admissibility, regular-simplex PSD and unit
  diagonal, correlation status, and exact normalization to matrix identity. Both its public target
  and reconnaissance bundle audit to exactly `propext`, `Classical.choice`, and `Quot.sound`; its
  warning-as-error, placeholder, unsafe, `native_decide`, and 100-column gates pass.
- Exact pinned-mathlib candidates used by the matrix prototype are `Matrix.PosSemidef.add`,
  `Matrix.PosSemidef.smul`, `Matrix.PosSemidef.of_dotProduct_mulVec_nonneg`,
  `Matrix.posSemidef_vecMulVec_self_star`, and `sq_sum_le_card_mul_sum_sq`.
- No packaged theorem for positivity of `I - J / m` was found. `Matrix.posSemidef_gram` was found
  but not needed. Directly casting `hm` into a real subtraction selected an unstable integer-sub
  path, so the prototype first casts `1 < m` and then uses ordered-field arithmetic. Identifying
  `allOnesMatrix` with `vecMulVec` also required explicit matrix extensionality rather than `simp`.
- For the common-Gaussian prototype, the equivalent scaled-shift representation is permitted:
  take `C ∼ N(0, 1/m)` and map `(ξ, C)` to `sqrt α • ξ + C • 1`. This has the same covariance as
  the source representation `sqrt α • (ξ + B • 1)` with `B ∼ N(0, 1/(m-1))`, while avoiding an
  extra scaling in the product-law proof. The production normalization proof retains the exact
  source representation; the scaled-shift variant remains independent cross-check evidence only.
- The independent scalar-algebra spike `/tmp/WP21MgfAlgebra.lean` is proof-complete (62 lines;
  SHA-256 `6b5b303a9754e0ef2115deb1a4016f0b1636be4a2e7d8caaff506cdacf4c09d3`) with sole import
  `Mathlib.Analysis.SpecialFunctions.Exp`. It verifies positivity of `α` and `sqrt α`, the
  identities `μ * sqrt α = λ` and `μ ^ 2 * α = λ ^ 2`, the exact Gaussian-prefactor equality,
  and cancellation by a positive exponential. Its warning-as-error, standard-axiom, trust, width,
  and whitespace gates pass.
- The stable cancellation declaration is `le_of_mul_le_mul_left h (Real.exp_pos a)`. The pinned
  API does not accept `(mul_le_mul_left (Real.exp_pos a)).mp` in this role. A directed calculation
  is also required for `Real.sq_sqrt`, because an unrestricted reverse rewrite reaches the square
  root in the denominator. Lean reserves Unicode `λ`, so implementation binders use `lam`.
- Common-Gaussian normalization reconnaissance is proof-complete in
  `/tmp/WP21Normalization.lean` (458 lines; SHA-256
  `2c1cf0ad47558b416f6e67f2950bfac69ec1ce262b9b80ba8ee7b2d73c74fdaf`). Its exact imports are
  `Maxima.ExponentialMoments`, `Mathlib.Algebra.Order.GroupWithZero.Finset`, and
  `Mathlib.MeasureTheory.Integral.Prod`. It proves the source-form Gaussian map law, maximum
  identities, all required exponential integrability, raw MGF factorization, and the exact
  prefactor identity. The factorization consumes its product-integrability witness through
  `integral_prod`, then uses `integral_const_mul` and `integral_mul_const`.
- Pinned declarations used by normalization include `Finset.sup'_add`, `Finset.mul₀_sup'`,
  `IsGaussian.ext`, `covariance_fst_snd_prod`, `covariance_eval_multivariateGaussian`,
  `covarianceBilin_multivariateGaussian`, `measurePreserving_fst`, `measurePreserving_snd`,
  `mgf_map`, `mgf_id_gaussianReal`, `integrable_exp_mul_gaussianReal`, and `Real.sq_sqrt`.
  No packaged multivariate-Gaussian sum law matching this coupling was found.
- Rejected normalization approaches include an unstable `ℝ≥0` result-type annotation, generic Pi
  simplification instead of `PiLp` application lemmas, unwitnessed `integral_prod_mul`, refolding
  an unfolded integral with `change`, and substituting `sqrt α ^ 2 = α` before ring-normalizing
  the nested covariance expression.
- The independent scaled-shift cross-check `/tmp/WP21GaussianLaw.lean` is proof-complete (192
  lines; SHA-256
  `d298cae281c58681b5a5dc1b963afad1a5677ed99ab95e2edae1c2cf1e2a177b`). With exact imports
  `Core.Matrix` and `Mathlib.Probability.Distributions.Gaussian.Multivariate`, it proves the same
  normalized Gaussian law using a common `N(0,1/m)` shift. Its warning-as-error, standard-axiom,
  trust, and width gates pass.
- The exact frozen theorem compiles in `/tmp/WP21Combined.lean` (84 lines; SHA-256
  `d058e40b2e69470b7fa49d187fa6f26617d6dccaa08e9b81c4969c5fbf0ed609`). It applies WP20 at
  `mu = lam / sqrt α`, rewrites both Gram matrices with the normalization identity, multiplies by
  the positive prefactor, and cancels it. Warning-as-error compilation passes; its axiom audit is
  exactly `propext`, `Classical.choice`, and `Quot.sound`; its placeholder and 100-column scans are
  clean.

## Completion report

- Production files and frozen hashes:
  - `Coding/Gram.lean`: 157 lines, SHA-256
    `5db8555409465876e09a25eab71865bb45dece2a4b8be2bd147d54cb72cd8db7`;
  - `Coding/Normalization.lean`: 441 lines, SHA-256
    `b536fe1a132ced1570dcd627795c33518ab5f4af256a143c39c18e84f8d71173`;
  - `Coding/RegularSimplex.lean`: 82 lines, SHA-256
    `502f1a19c214497b175dd2caa2c3cb361c0f1734ccf085c1853eb6c09cab933a`.
- The exported surface is exactly `regularSimplexGram`, `gramNormalization`,
  `gramMgf_normalization_identity`, and `gramGaussianMax_mgf_le_regularSimplex`. All other WP21
  declarations are private.
- All three direct warning-as-error checks and their named target builds pass. The public root and
  full `lake build --wfail` pass with 3,232 jobs.
- The permanent audit contains 65 active declarations. Every result, including all four WP21
  declarations, is exactly `propext`, `Classical.choice`, and `Quot.sound`.
- Trusted-source scanner tests pass 10/10 and the production scanner exits zero. Placeholder,
  project-axiom, unsafe, `native_decide`, forbidden-import, 100-column, trailing-whitespace, and
  `git diff --check` gates are clean.
- Independent cross-review certified the matrix scale, PSD and diagonal proofs, normalized
  admissibility, common-Gaussian variance and covariance, maximum identities, exponential
  integrability, witnessed Fubini factorization, Gaussian MGF coefficient, prefactor algebra,
  regular-simplex normalization, WP20 application, and final positive cancellation without defect.
- No commit was made, as required by project policy.
