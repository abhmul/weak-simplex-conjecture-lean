# U07 — Shared adaptive orthant reduction

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, Section 3.6, U07, and 10
- **Mathematical source:** `notes/simplex_optimality_proof.v3.md`, Sections 8.2 and 9, especially Lemma 9.1
- **Owned Lean files:** new `WeakSimplexConjectureLean/Orthant/AdaptiveReduction.lean`; edit `WeakSimplexConjectureLean/Orthant/PositiveDefiniteConditional.lean`
- **Integration-owned files:** this card, the public root, and `WeakSimplexConjectureLean/Audit/Axioms.lean`
- **Acceptance theorem:** the frozen `WeakSimplex.lowerOrthant_ge_iid_of_posDef_of_centeredProduct`
- **Prerequisites:** the existing adaptive-witness, Gaussian-shift, and tilted-half-line modules

## Frozen shared API

```lean
theorem lowerOrthant_eq_adaptiveProduct
    {m : ℕ} (R : Matrix (Fin m) (Fin m) ℝ) (hR : R.PosSemidef)
    (c : ℝ) (w : AdaptiveWitnesses R c) :
    (multivariateGaussian 0 R) (lowerOrthant c) =
      ENNReal.ofReal
        (Real.exp (-qform R w.a / 2) *
          ∫ x, ∏ i, centeredTiltedHalfLine (w.s i) (x i)
            ∂multivariateGaussian 0 R)
```

Expose the existing product-mass identity as `adaptiveProduct_mass_eq` and the exponential witness-value inequality as `adaptiveValue_exp_bound`. The acceptance theorem's statement must remain byte-for-byte unchanged through its proposition.

## Method

Move the covariance-independent endpoint, measurability, boundedness, product-mass, value, and Gaussian-shift calculations below the conditional theorem. The only covariance assumption in `lowerOrthant_eq_adaptiveProduct` is positive semidefiniteness, required by the Gaussian Esscher identity. Rewrite the old positive-definite theorem as witness construction plus the centered-product comparison plus these shared lemmas.

## Search and prototype record

The required declarations already compile inside `PositiveDefiniteConditional.lean`; this package is an ownership and assumption audit rather than a new API search. Verify every extracted helper's actual dependency while moving it and avoid broadening imports.

## Implementation log

The endpoint and Gaussian-shift algebra now lives in `Orthant/AdaptiveReduction.lean`. The shared reduction assumes only `R.PosSemidef`; the mass and exponential-value lemmas are public for U09. `PositiveDefiniteConditional.lean` constructs the existing positive-definite witness, invokes the centered-product parameter, and closes through the shared lemmas. Its public proposition is textually identical to the release baseline. Both file gates and the 3,238-job build pass; the permanent audit reports exactly the accepted three foundational axioms for all four load-bearing declarations.

## Forbidden shortcuts

- no statement change to the acceptance theorem;
- no duplicate independent proof retained in the old module;
- no positive-definiteness assumption on the shared reduction theorem;
- no placeholders or project axioms.

## Acceptance and audit

Compile both files, run the full build, and compare the acceptance theorem declaration before and after. Audit the three shared load-bearing lemmas and the acceptance theorem.
