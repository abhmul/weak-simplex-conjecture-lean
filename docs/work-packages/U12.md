# U12 — Operational uniqueness

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, Sections 2.7, 3.8, U12, and 10–11
- **Mathematical source:** `notes/simplex_optimality_proof.v3.md`, Sections 3 and 4; the governing plan supplies equality characterization
- **Owned Lean file:** `WeakSimplexConjectureLean/Coding/WeakSimplex.lean`
- **Integration-owned files:** this card and `WeakSimplexConjectureLean/Audit/Axioms.lean`
- **Acceptance theorem:** `WeakSimplex.weak_simplex_eq_iff_codeGram_eq_of_scoreMaximizingDecoders`
- **Prerequisite:** U11

## Frozen value API

Retain the exact `bayesValue_lt_regularSimplex` and `bayesValue_eq_regularSimplex_iff` statements in Section 2.7 of the governing plan. Both require `hlam : 0 < lam`.

## Frozen deterministic-decoder API

```lean
theorem weak_simplex_strict
    {n : ℕ} (hn : 0 < n)
    (code simplex : Fin (n + 1) → Coord n)
    (hunit : ∀ i, ‖code i‖ = 1)
    (hsimplex : codeGram simplex = regularSimplexGram (n + 1))
    (hcode_ne : codeGram code ≠ regularSimplexGram (n + 1))
    (lam : ℝ) (hlam : 0 < lam) :
    decoderSuccess (Nat.succ_pos n) code lam <
      decoderSuccess (Nat.succ_pos n) simplex lam

theorem weak_simplex_eq_iff_codeGram_eq
    {n : ℕ} (hn : 0 < n)
    (code simplex : Fin (n + 1) → Coord n)
    (hunit : ∀ i, ‖code i‖ = 1)
    (hsimplex : codeGram simplex = regularSimplexGram (n + 1))
    (lam : ℝ) (hlam : 0 < lam) :
    decoderSuccess (Nat.succ_pos n) code lam =
        decoderSuccess (Nat.succ_pos n) simplex lam ↔
      codeGram code = regularSimplexGram (n + 1)
```

## Frozen arbitrary-decoder API

```lean
theorem weak_simplex_strict_of_scoreMaximizingDecoders
    {n : ℕ} (hn : 0 < n)
    (code simplex : Fin (n + 1) → Coord n)
    (hunit : ∀ i, ‖code i‖ = 1)
    (hsimplex : codeGram simplex = regularSimplexGram (n + 1))
    (hcode_ne : codeGram code ≠ regularSimplexGram (n + 1))
    (lam : ℝ) (hlam : 0 < lam)
    (decoder simplexDecoder : Coord n → Fin (n + 1))
    (hdecoder_meas : Measurable decoder)
    (hdecoder_max : IsScoreMaximizingDecoder code decoder)
    (hsimplexDecoder_meas : Measurable simplexDecoder)
    (hsimplexDecoder_max : IsScoreMaximizingDecoder simplex simplexDecoder) :
    decoderSuccessOf code lam decoder <
      decoderSuccessOf simplex lam simplexDecoder

theorem weak_simplex_eq_iff_codeGram_eq_of_scoreMaximizingDecoders
    {n : ℕ} (hn : 0 < n)
    (code simplex : Fin (n + 1) → Coord n)
    (hunit : ∀ i, ‖code i‖ = 1)
    (hsimplex : codeGram simplex = regularSimplexGram (n + 1))
    (lam : ℝ) (hlam : 0 < lam)
    (decoder simplexDecoder : Coord n → Fin (n + 1))
    (hdecoder_meas : Measurable decoder)
    (hdecoder_max : IsScoreMaximizingDecoder code decoder)
    (hsimplexDecoder_meas : Measurable simplexDecoder)
    (hsimplexDecoder_max : IsScoreMaximizingDecoder simplex simplexDecoder) :
    decoderSuccessOf code lam decoder =
        decoderSuccessOf simplex lam simplexDecoder ↔
      codeGram code = regularSimplexGram (n + 1)
```

## Method

Transfer U11 through `bayesValue_eq_gramMgf` and the strictly positive Bayes prefactor. Then rewrite deterministic and arbitrary measurable score-maximizing decoder success using the existing Bayes-value identities. Derive all iff theorems by cases on Gram equality; do not add tie-breaking assumptions beyond the existing interfaces.

## Forbidden shortcuts

- no theorem at `lam = 0`;
- no equality conclusion weaker than Gram-matrix equality;
- no injectivity, spanning, or congruence assumption on code realizations;
- no change to the old non-strict theorem statements;
- no placeholders or project axioms.

## Acceptance and audit

Compile the file, run the full build, and audit both Bayes theorems plus all four decoder declarations. The arbitrary measurable score-maximizing iff theorem is the package acceptance theorem.

## Completion log

- The Bayes strict and equality theorems transfer U11 through `bayesValue_eq_gramMgf` using the explicitly positive Bayes prefactor.
- Deterministic ML and arbitrary measurable score-maximizing decoder results rewrite through the existing Bayes-value identities; no tie-breaking, realization, or geometric assumptions were added, and every new theorem requires `0 < lam`.
- The edited file and public root compile, `lake build` passes all 3244 jobs, and the permanent audit passes for 139 declarations with exactly the accepted foundational axioms.
- Existing non-strict APIs and production placeholder/project-axiom/`unsafe`/`native_decide`, width, and whitespace scans are clean.
