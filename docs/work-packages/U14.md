# U14 — Audit, documentation, and release readiness

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, U14 and Sections 10–12
- **Owned files:** `WeakSimplexConjectureLean.lean`, `WeakSimplexConjectureLean/Audit/Axioms.lean`, `scripts/check_axiom_audit.py`, `scripts/check_uniqueness_sanity.py`, `scripts/test_check_uniqueness_sanity.py`, `README.md`, `docs/theorem-dependencies.md`, and release notes if present
- **Acceptance deliverable:** the complete uniqueness extension passes every build, trust, API-regression, and documentation gate
- **Prerequisite:** U12; U13 is optional and does not block this package

## Required audit coverage

Add every new public declaration listed in Section 10.2 of the governing plan to the permanent dossier. Extend `REQUIRED_DECLARATIONS` in `scripts/check_axiom_audit.py` with the load-bearing strict lower-orthant, strict/equality MGF, Gram, Bayes, deterministic decoder, and arbitrary decoder acceptance theorems. Exact accepted axioms remain `propext`, `Classical.choice`, and `Quot.sound`.

## Documentation requirements

Update the README and theorem dependency graph only after the theorems compile. State that uniqueness holds for positive signal strength and is certified at the Gram-matrix level. Explain the two narrow singular exceptions without suggesting a general singular centered-product theorem. Keep geometric congruence explicitly optional.

## Regression requirements

Verify the frozen statements named in Section 10.4 against baseline `b86317606e04b8956611b7dacbf822112b97ea66`. Run the full build, the axiom script, the production placeholder/axiom/unsafe/native-decision scans, import-graph and provenance checks used by CI, line-width and whitespace checks, and a read-only final review. Do not create a commit under the repository's no-autonomous-commit policy.

Retain the deterministic external sanity checks required by Section 10.3 and label them explicitly as numerical diagnostics outside the trusted Lean proof boundary.

## Completion log

- The README and theorem dependency graph document strictness, positive-signal uniqueness at the Gram-matrix level, arbitrary measurable score-maximizing decoders, the two narrow singular bridges, and the optional status of geometric congruence.
- All nine public statements frozen by Section 10.4 match baseline `b86317606e04b8956611b7dacbf822112b97ea66` byte-for-byte; all 26 pre-existing public declaration headers in modified production modules also match, with no removal or rename.
- `lake build --wfail` passes all 3244 jobs, the public root compiles with warnings as errors, and the permanent audit passes for 139 declarations with exactly `propext`, `Classical.choice`, and `Quot.sound`.
- All 50 repository Python tests, the trusted-source scanner, import-graph check, exact vendor-provenance check, production trust scans, 100-column check, whitespace check, and `git diff --check` pass.
- The retained deterministic numerical suite passes all 18 singular, positive-definite, strict, equality, and zero-signal diagnostics; it is explicitly outside the trusted proof boundary.
- The project formalization skill validates after being generalized to resolve work packages through canonical governing plans rather than a fixed package range or duplicated plan snapshot.
- Independent read-only reviews found no proof, API, import, trust, or documentation blocker. Optional U13 orthogonal congruence remains deferred and does not block the certified Gram-level theorem.
