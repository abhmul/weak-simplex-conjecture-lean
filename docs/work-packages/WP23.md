# WP23 — Audit and publication preparation

- **ID:** WP23
- **Status:** complete; M5 closed
- **Reconnaissance owners:** assigned disjoint read-only audits for provenance, public theorem
  documentation, and reproducibility/import hygiene
- **Integration owner:** `/root`
- **Owned integration files:** `README.md`, `PROVENANCE.md`, `WeakSimplexConjectureLean.lean`,
  `WeakSimplexConjectureLean/Audit/Axioms.lean`, vendor attribution documentation, this card, and
  the daily note
- **Stop budget:** one bounded publication pass: one hour each for provenance, theorem mapping,
  clean-snapshot reproduction, and final integration review
- **Upstream acceptance theorem:** `WeakSimplex.weak_simplex`
- **Acceptance deliverable:** the M5 repository audit dossier; WP23 introduces no new mathematical
  theorem

## Statement

WP23 is a publication/infrastructure package and therefore has no new Lean acceptance theorem.
Its acceptance object is the already proved `WeakSimplex.weak_simplex` together with a complete,
reproducible audit dossier. This is the same infrastructure exception used by WP00.

## Role and governing criteria

WP23 closes the report's M5 criteria:

- a clean snapshot can fetch pinned dependencies and run `lake build`;
- dependency inputs and vendored upstream commits are pinned;
- every vendored production file has complete provenance and license metadata;
- the permanent axiom audit covers every public milestone theorem;
- trusted production source contains no placeholder, project axiom, `unsafe`, `native_decide`, or
  Scratch/Scaffold import;
- README contains reproduction instructions and a theorem dependency map;
- independent reviewers verify the final public surface and proof directions.

Relevant report sections are §§2.3 (M5), 9–10, 12 (WP23), 14–15, and 18. No mathematical-source
section is newly consumed; all mathematical acceptance theorems were frozen in WP01–WP22.

## Reconnaissance split

1. Audit every file under `WeakSimplexConjectureLean/Vendor/` against `PROVENANCE.md`, including
   hashes, upstream repository/commit/path, license copy, local changes, build command, transitive
   closure, and axiom result.
2. Audit the public root, permanent axiom dossier, README status, theorem dependency map, and
   reproduction commands against the actual compiled declaration graph.
3. Audit Scratch/Scaffold isolation, import reachability, trusted-source scans, manifest/toolchain
   pins, CI commands, and a clean-snapshot build strategy.
4. Integrate only evidence-backed documentation changes, rerun all gates, and record exact external
   blockers such as a project-license choice or hosted-CI observation.

## Forbidden shortcuts

- Do not change mathematical theorem statements or proof bodies in this package.
- Do not delete Scratch or other user work merely to make a scan clean.
- Do not claim a clean-clone result from the dirty working directory or from reused `.olean` files.
- Do not synthesize or select a project license without user authorization.
- Do not alter vendored code merely to simplify provenance review.
- Do not add external dependencies, project axioms, placeholders, or autonomous commits.

## Acceptance commands

```bash
lean --version
lake --version
lake build --wfail
lake env lean -DwarningAsError=true WeakSimplexConjectureLean/Audit/Axioms.lean
python3 -m unittest discover -s scripts -p 'test_audit_trusted_lean.py'
python3 scripts/audit_trusted_lean.py \
  --public-root WeakSimplexConjectureLean.lean \
  WeakSimplexConjectureLean WeakSimplexConjectureLean.lean
```

The clean-snapshot test must use a temporary directory with no copied `.lake`, `.olean`, or `.ilean`
artifacts. It may reuse the pinned network cache only if that reuse is disclosed; otherwise run
`lake update` followed by `lake build --wfail` from the snapshot.

## Reconnaissance log

- WP23 starts only after WP22 closes M4 with three clean independent reviews, a 3,235-job full
  warning-as-error build, an 89-declaration standard-axiom audit, and clean trusted-source scans.
- The public-root audit found all 21 mathematical acceptance declarations from WP02--WP22 present
  under their frozen names and represented in the permanent dossier. WP00, WP01, and WP23 are
  infrastructure/reconnaissance exceptions without new mathematical acceptance theorems.
- The former audit-module introduction was stale WP00 future tense. It now describes the active
  permanent dossier, and WP00 has an explicit complete status header.
- The README now includes an exact theorem dependency map from the adaptive and product branches
  through the singular orthant theorem, MGF normalization, Bayes identity, operational decoder,
  and `weak_simplex`. It continues to state the supplied-realization caveat explicitly.
- Local StatLean evidence at commit `31c61ed887bf3be0def314a3b3e5375d203b5ba1` verifies every
  upstream path, whole/slice hash, local diff, import closure, license copy, and table entry. All
  five vendored Lean files compile warning-as-error and pass the trusted-source scan.
- Two vendor helper tops that were only transitively audited now have direct permanent entries:
  `pi_withDensity_prod` and `withDensity_map_eq_map_withDensity`. The dossier therefore has 91
  active declarations, all exactly `[propext, Classical.choice, Quot.sound]`.
- The Gaussian-shift vendor attribution now records its prose scanner-token change, with current
  local SHA-256 `92e52fc007d5bb510709e00f8424c4b6de7eeb8c11bb4972c6cb36bd0cc2153c`.
  The provenance ledger records the same hash and the common clean-scan result; the vendor README
  accurately limits its coverage claim to externally sourced files.
- A fresh 162-file source snapshot copied no `.git`, `.lake`, `.olean`, or `.ilean` artifacts. With
  a task-local `XDG_CACHE_HOME`, `lake update` resolved mathlib exactly to
  `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f` and fetched 8,542 binary-cache files. The snapshot
  passed `lake build --wfail` with 3,235 jobs, direct warning-as-error public-root and axiom-audit
  checks, all 91 standard-axiom entries, scanner tests, and the official production scan.
- The generated import graph reaches all 47 production modules plus the public root and contains
  no Scratch or Scaffold node. No production source imports Scratch/Scaffold and no compiled
  artifact is tracked.
- A synthetic production probe first exposed missing root-level Scratch/Scaffold import and
  `native_decide` checks. Successive independent probes then found literal/interpolation
  desynchronization, escaped-identifier delimiters, raw-term reparsing, numeric/character token
  fusion, and absolute-ancestor path classification. The final scanner retains normal, raw, and
  escaped-identifier text for token checking; treats character literals atomically; masks only
  comment text recognized by both ordinary and potential-interpolation structural views; follows
  Lean numeric and raw-token boundaries; and classifies nonproduction paths relative to the project
  root. Its 30-test suite,
  official repository scan, compiling adversarial probes, and two bounded final rereviews pass.
- The owner selected the MIT license and authorized a coherent commit and push. The root license,
  Lake package metadata, README, and mixed-license provenance agree. The machine-specific Codex
  configuration was removed from tracking while its sanitized example remains.
- Commit `7d48b070c9a474de78bae2e47d458861b30d940a` was cloned with `--no-local` into an
  artifact-free directory and tested with a separate initially empty XDG cache. `lake update`
  checked out mathlib at `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f` and fetched all 8,542
  cache artifacts. The clone passed the 3,235-job warning-as-error build, public-root and 91-entry
  standard-axiom checks, 30 scanner tests, official production scan, six-artifact provenance gate,
  and 48-node import graph with no Scratch/Scaffold node. The clone remained Git-clean.

## Completion report

- Local status: all source, build, trust, provenance, theorem-map, source-snapshot, and independent
  final-review gates are complete.
- M5 status: complete. The MIT-licensed coherent release commit passed literal clean-clone
  verification. Pushing the branch and observing hosted CI remain release-distribution tasks
  contingent on configuring a safe GitHub remote.
