# U11 — Gram rigidity and strict Gaussian-max comparison

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, Sections 2.7, 3.8, U11, and 10
- **Mathematical source:** `notes/simplex_optimality_proof.v3.md`, Sections 4.1–4.2; the equality algebra is frozen by the governing plan
- **Owned Lean files:** `WeakSimplexConjectureLean/Coding/Gram.lean` and `WeakSimplexConjectureLean/Coding/RegularSimplex.lean`
- **Integration-owned files:** this card and `WeakSimplexConjectureLean/Audit/Axioms.lean`
- **Acceptance theorem:** `WeakSimplex.gramGaussianMax_mgf_lt_regularSimplex`
- **Prerequisite:** U10

## Frozen public API

```lean
theorem gramNormalization_eq_one_iff
    {m : ℕ} (hm : 1 < m)
    (G : Matrix (Fin m) (Fin m) ℝ) :
    ((((m : ℝ) - 1) / (m : ℝ)) • G +
        (1 / (m : ℝ)) • allOnesMatrix m =
      (1 : Matrix (Fin m) (Fin m) ℝ)) ↔
      G = regularSimplexGram m

theorem gramGaussianMax_mgf_lt_regularSimplex
    {m : ℕ} (hm : 1 < m)
    (G : Matrix (Fin m) (Fin m) ℝ)
    (hG : IsCorrelation G)
    (hGne : G ≠ regularSimplexGram m)
    (lam : ℝ) (hlam : 0 < lam) :
    mgf (coordinateMax (Nat.zero_lt_of_lt hm))
        (multivariateGaussian 0 G) lam <
      mgf (coordinateMax (Nat.zero_lt_of_lt hm))
        (multivariateGaussian 0 (regularSimplexGram m)) lam

theorem gramGaussianMax_mgf_eq_regularSimplex_iff
    {m : ℕ} (hm : 1 < m)
    (G : Matrix (Fin m) (Fin m) ℝ)
    (hG : IsCorrelation G)
    (lam : ℝ) (hlam : 0 < lam) :
    mgf (coordinateMax (Nat.zero_lt_of_lt hm))
          (multivariateGaussian 0 G) lam =
        mgf (coordinateMax (Nat.zero_lt_of_lt hm))
          (multivariateGaussian 0 (regularSimplexGram m)) lam ↔
      G = regularSimplexGram m
```

## Method

Prove the expanded normalization iff by matrix extensionality and field algebra without exposing the private normalization definitions. Use it to turn `G ≠ regularSimplexGram m` into nonidentity of the normalized admissible covariance, apply U10 at the positive rescaled parameter, and transfer strictness through the positive exponential prefactor and the existing normalization identity. Close the equality theorem by cases.

## Forbidden shortcuts

- no stronger rank or realization assumption on `G`;
- no refactor of private normalization definitions unless required by a measured elaboration problem;
- no equality theorem at `lam = 0`;
- no placeholders or project axioms.

## Acceptance and audit

Compile both files, run the full build, and audit all three declarations. Preserve the old non-strict Gram theorem exactly.

## Completion log

- The expanded normalization iff is proved by matrix extensionality and field algebra without exposing or refactoring the private normalization definitions.
- The strict Gram-MGF theorem applies U10 at the positive rescaled parameter and cancels only strictly positive exponential prefactors; the equality theorem is the prescribed case split.
- Both edited files compile, `lake build` passes all 3244 jobs, and the permanent audit passes for 133 declarations with exactly the accepted foundational axioms.
- Existing non-strict statements and production trust/width/whitespace scans are clean.
