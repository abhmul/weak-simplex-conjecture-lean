# U00 — Baseline freeze and rigidity-branch preparation

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, Sections 0, 1, and U00
- **Baseline:** `b86317606e04b8956611b7dacbf822112b97ea66` (`v0.1.0`)
- **Owned files:** governing documents and the repository-specific formalization skill; no production Lean files
- **Acceptance deliverable:** the unchanged release baseline passes the full build, trust audit, and trusted-source scans

## Constraints

- Preserve every completed public theorem statement.
- Do not commit autonomously.
- Record missing or mismatched mathematical provenance explicitly.
- Keep the formalization skill independent of a fixed package-number series or one extension architecture.

## Completion record

- `lean --version` reported Lean 4.31.0 and `lake --version` reported Lake 5.0.0.
- `lake-manifest.json` pins mathlib commit `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f` from input `v4.31.0`.
- `lake build` completed successfully for 3235 jobs.
- `python3 scripts/check_axiom_audit.py` passed for 98 declarations, each with exactly `propext`, `Classical.choice`, and `Quot.sound`.
- Trusted production scans found no `sorry`, `admit`, project `axiom`, or `unsafe` declaration.
- The plan's original baseline hash was absent from the repository; the tagged release baseline above is the verified replacement.
- No uniqueness-revision manuscript was present locally, and the public arXiv record was still v1. The uniqueness dossier is therefore the operative mathematical specification pending an independent source-level review.
- Lean LSP MCP 0.28.1 was started against the pinned project and its read-only tool surface was verified.

## Handoff

Proceed to U01 using `docs/work-packages/U01.md`. The source-provenance review item does not authorize changing a frozen theorem; any discrepancy found later must be handled as an architecture decision.
