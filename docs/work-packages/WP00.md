# WP00 — Baseline, CI, and agent configuration

- **ID:** WP00
- **Status:** complete
- **Owner:** integration owner (Codex)
- **Owned files:**
  - `.github/workflows/lean_action_ci.yml`
  - `.gitignore`
  - `README.md`
  - `PROVENANCE.md`
  - `WeakSimplexConjectureLean.lean`
  - `WeakSimplexConjectureLean/Basic.lean`
  - `WeakSimplexConjectureLean/Audit/Axioms.lean`
  - `WeakSimplexConjectureLean/Vendor/README.md`
  - `scripts/audit_trusted_lean.py`
  - `scripts/test_audit_trusted_lean.py`
  - `docs/work-packages/WP00.md`
  - `daily-notes/2026-07-17.md`
- **Stop budget:** one bounded WP00 closeout pass; stop before WP01–WP03 theorem work
- **Upstream acceptance theorems:** none
- **Allowed Lean imports:** `WeakSimplexConjectureLean.Basic` in the public root and `WeakSimplexConjectureLean` in `Audit/Axioms.lean`; no imports in `Basic.lean`
- **Acceptance deliverable:** the pinned Lean 4.31.0 project, LSP diagnostics, public root, audit skeleton, provenance gate, and CI trust checks all pass without claiming a mathematical theorem

## Statement

WP00 is configuration-only. It has no Lean acceptance theorem. This explicit deliverable is the exception permitted by the work-package template for infrastructure spikes.

## Mathematical role

Close the reproducible-foundation edge required before any mathematical work package begins.

Relevant plan sections: §§2.3 (M0), 8, 9, 10, 12 (WP00), 14, and 17 (days 1–2). No mathematical-source section is applicable.

## Search record

| Query or declaration | Result | Reused? |
|---|---|---|
| Lean LSP diagnostics on `WeakSimplexConjectureLean/Basic.lean` | Available; no diagnostics | Yes |
| [`leanprover/lean-action` official README](https://github.com/leanprover/lean-action) | `build`, `build-args`, and post-action Lean steps are supported | Yes |
| `lake exe graph --help` | Supports a downstream `--to WeakSimplexConjectureLean` graph | Yes |
| Pinned syntax probes for `private axiom`, `public unsafe`, `public import`, and same-line doc comments | Anchored grep patterns had false negatives; replaced by a tested lexical scanner | No |
| Pinned `Lean.Parser.Module.Syntax` import grammar and `Init.Meta.Defs` identifier predicates | Used for escaped/multiline imports and exact identifier boundaries in the scanner | Yes |
| Repository scan for a current work-package card | Only the blank template existed | No |
| Repository scan for `Audit/Axioms.lean` and `PROVENANCE.md` | Both production files were absent | No |

## Implementation constraints

- Do not introduce or freeze mathematical theorem statements.
- Do not add external Lean code or dependencies.
- Do not import `Scratch/`, `Scaffold/`, or `Vendor/` from the public root.
- Keep the existing pinned toolchain, manifest, and Lake options unchanged.
- Do not commit autonomously.

The report's days 1–2 module-skeleton item is intentionally limited here to the public root, `Audit/`, and `Vendor/`. Empty branch modules and speculative theorem headers would conflict with the report's later API-spike and statement-freeze gates. Concrete branch modules belong to their owning WP01–WP03 cards. Consequently WP00 may close, but M0 remains open until its theorem-interface criterion is met.

## Validation

```bash
lean --version
lake --version
lake env lean WeakSimplexConjectureLean/Basic.lean
lake env lean WeakSimplexConjectureLean/Audit/Axioms.lean
lake env lean WeakSimplexConjectureLean.lean
lake build
```

```bash
python3 -m unittest discover -s scripts -p 'test_audit_trusted_lean.py'
python3 scripts/audit_trusted_lean.py --public-root WeakSimplexConjectureLean.lean WeakSimplexConjectureLean WeakSimplexConjectureLean.lean
```

There is no `#print axioms` target until a milestone theorem is introduced. The audit skeleton must list the five future milestone checks without pretending that they already exist.

## Completion report

- Files changed: `.github/workflows/lean_action_ci.yml`, `.gitignore`, `README.md`, `PROVENANCE.md`, `WeakSimplexConjectureLean.lean`, `WeakSimplexConjectureLean/Basic.lean`, `WeakSimplexConjectureLean/Audit/Axioms.lean`, `WeakSimplexConjectureLean/Vendor/README.md`, `scripts/audit_trusted_lean.py`, `scripts/test_audit_trusted_lean.py`, this card, and `daily-notes/2026-07-17.md`
- Build result: local file gates, `lake build --wfail`, the public-root import graph, workflow YAML parsing, scanner unit tests, and all trust/import/provenance checks passed; the first hosted GitHub Actions run remains unobserved
- Axiom result: no acceptance theorem exists in WP00; the hardened source scan found no project `axiom`/`constant` or `unsafe` declaration, and the future milestone audit skeleton compiles
- Placeholders remaining: none in trusted Lean source
- External code used and provenance: no external Lean code; the official `leanprover/lean-action` README was used to verify CI inputs
- Exact blockers: none for WP00; M0 remains open because no mathematical theorem interfaces have yet been frozen or compiled
- Recommended next package: instantiate disjoint cards for Wave A (WP01 external/API spikes, WP02 core wrappers, and WP03 normal calculus)
