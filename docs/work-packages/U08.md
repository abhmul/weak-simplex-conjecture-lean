# U08 — Strict product for adaptive half-lines

- **Status:** complete
- **Governing plan:** `docs/weak_simplex_lean_uniqueness_formalization_plan.v1.md`, Sections 2.3, 3.5, U08, and 10
- **Mathematical source:** `notes/simplex_optimality_proof.v3.md`, Section 6.2 supplies the non-strict rotation; the governing uniqueness plan is the sole source for the strictness argument
- **Owned Lean files:** `WeakSimplexConjectureLean/Product/SumDifference.lean` for the frozen seam and new `WeakSimplexConjectureLean/Rigidity/TiltedHalfLineProduct.lean`
- **Integration-owned files:** this card, the public root, and `WeakSimplexConjectureLean/Audit/Axioms.lean`
- **Acceptance theorem:** `WeakSimplex.centeredTiltedHalfLine_product_lt_of_ne_one`
- **Prerequisites:** U03, U04, U05, and the completed `WeakSimplex.lowerOrthant_ge_iid`

## Frozen public API

```lean
/-- The adaptive centered half-lines have a strict product gain away from independence. -/
theorem centeredTiltedHalfLine_product_lt_of_ne_one
    {m : ℕ} (hm : 0 < m)
    (R : Matrix (Fin m) (Fin m) ℝ)
    (hR : IsWeakSimplexCov R)
    (hRne : R ≠ (1 : Matrix (Fin m) (Fin m) ℝ))
    (s : Coord m) :
    (∏ i, ∫ z, centeredTiltedHalfLine (s i) z ∂gaussianReal 0 1) <
      ∫ x, ∏ i, centeredTiltedHalfLine (s i) (x i)
        ∂multivariateGaussian 0 R
```

Promote `fixedAverageFactor`, `sumDifferenceProduct`, and `integral_fixedAverageFactor_eq_normalizedSelfConvolution` from `Product/SumDifference.lean`. Add the following packaged theorem identifying the iterated sum–difference integral with the square of the original correlated product integral. Keep all lower rotation plumbing private.

```lean
theorem integral_integral_sumDifferenceProduct_eq_sq
    {m : ℕ}
    (R : Matrix (Fin m) (Fin m) ℝ)
    (h : Fin m → ℝ → ℝ)
    (hh_meas : ∀ i, Measurable (h i))
    (hh_bounded : ∀ i, Bornology.IsBounded (Set.range (h i))) :
    (∫ u, ∫ v, sumDifferenceProduct h (u, v)
        ∂multivariateGaussian 0 R ∂multivariateGaussian 0 R) =
      (∫ x, ∏ i, h i (x i) ∂multivariateGaussian 0 R) ^ 2
```

The public identity `normalizedCenteredTiltedHalfLine_product` in `Tilt/NormalizedTiltedHalfLine.lean` is the frozen pointwise specialization used to rewrite the adaptive factors; do not duplicate its proof.

## Method

Normalize the adaptive factors. For fixed average `u`, rewrite each `fixedAverageFactor` as a positive scalar times a symmetric-interval indicator of radius `Real.sqrt 2 * H (s i) - u i`. Apply U02 everywhere and U03 on the open strictness set. The zero-threshold lower orthant lies in that set by `H_pos`, and the completed non-strict theorem gives it positive probability. Strictly integrate, use the packaged rotation identity, and combine U04 applied to the continuous self-convolutions with normalization cancellation.

## Search and prototype record

Pinned-mathlib reconnaissance found `lintegral_strict_mono_of_ae_le_of_ae_lt_on` and the existing project rotation machinery. No external strict-product theorem is needed. Prototype the exact fixed-average indicator identity and the strict-integral conversion before editing the integration seam.

## Forbidden shortcuts

- no import from `Orthant/Strict.lean` or any future strict orthant result;
- no whole singular centered-product theorem;
- no generic equality theory for log-concave factors;
- no duplicate copy of the full sum–difference rotation proof;
- no placeholders or project axioms.

## Acceptance and audit

Compile both owned files, run the full build, and audit the packaged rotation identity and acceptance theorem. Verify that the import path through the old non-strict singular orthant module remains acyclic.

## Completion log

- The frozen sum--difference seam and its packaged iterated-integral identity compile; the old non-strict deficit theorem now consumes that identity without changing its statement.
- The fixed-average proof handles nonnegative radii by U02, positive radii by U03, and a negative radius by the vanishing coordinate factor.
- The strict average set contains `lowerOrthant 0`; `H_pos` and `lowerOrthant_ge_iid` give it positive Gaussian mass.
- The acceptance theorem compiles through U04 and normalization cancellation. `lake build` passes all 3241 jobs, and the permanent audit passes for 124 declarations with exactly the accepted foundational axioms.
- Production placeholder, project-axiom, `unsafe`, `native_decide`, width, and whitespace scans are clean for the owned files.
